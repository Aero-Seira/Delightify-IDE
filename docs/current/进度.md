# 进度日志

> 滚动记录关键决策与已落地的工作。最新在上。

## 2026-06-15 · MVP-0 浏览层对齐与真实快照 smoke

- 提交 `da398a7` 已完成 `docs/current/mvp0-implementation-plan.md` 的 Task 1-7：
  - ItemBrowser 显示名/按名搜索改走 `items.translation_key -> translations`，支持 `zh_cn -> en_us` fallback，并加语言选择器。
  - `recipes:get-detail` 返回 `{ recipe, inputs, outputs }`，RecipeBrowser 详情优先展示结构化 `recipe_inputs/recipe_outputs`。
  - `unparsed` 或 `raw_json` 为空的配方显示“未结构化/脚本配方”徽标，不再只显示 `?`。
  - KubeJS emitter 仅补 `replaceOutput` 保守注释，不把输出替换纳入 change set。
  - 导入失败会静默写入 `data_imports(is_success=0, error_message, 计数=0)`。
  - 新增 `examples/export-snapshots/` 作为真实 exporter 快照本地放置目录；目录进 Git，`export.sqlite` 默认忽略。
- 验证通过：
  - `pnpm typecheck`
  - `pnpm build`
  - `pnpm smoke:mvp0`
  - `pnpm exporter:build`
  - 构造缺表 SQLite 后，确认 `data_imports` 出现 `is_success=0` 行。
- 已放入真实快照 `examples/export-snapshots/export.sqlite`（被 `.gitignore` 忽略，不纳入 Git）并完成验证：
  - `PRAGMA integrity_check = ok`
  - 数据规模：194 mods、8295 items、6488 blocks、10341 recipes、31301 recipe_inputs、8005 recipe_outputs、365731 translations。
  - `item_resources`、`recipe_views`、`recipe_view_backgrounds` 为空，符合 exporter v1 当前事实。
  - `pnpm smoke:mvp0 --data-file examples/export-snapshots/export.sqlite --query 冷杉木板 --target biomesoplenty:fir_planks` 通过：命中 `biomesoplenty:fir_planks`、`biomeswevegone:fir_planks`，dry-run 生成 361 条 diff，因风险/输出/引用规则无自动 change set，KubeJS 导出按预期跳过。
- 注意：该快照没有精确 `zh_cn = 铜锭` 的 item translation；默认文档示例 `铜锭` 不适合作为此快照的 smoke query。

## 2026-06-14 · 过时文档归档

- `docs/current/现状审计.md` 与一次性启动清单已移入 `docs/archive/audits/`。
- 旧 `docs/reference/` 规划文档已按类别移入 `docs/archive/planning/`、`docs/archive/import-engine/`、`docs/archive/completed/`。
- 当前文档入口收敛为：`CLAUDE.md` → `docs/README.md` → `docs/current/` / `docs/spec-snapshot/`；归档文档只作背景。

## 2026-06-14 · exporter 并入 Delightify monorepo

- NeoForge exporter 已从上级 `../modpack-ide-exporter` 迁入 `packages/exporter`。
- 新增根命令：`pnpm exporter:build`、`pnpm exporter:compile`、`pnpm exporter:runClient`、`pnpm exporter:runServer`、`pnpm exporter:clean`。
- 默认 `pnpm build` 仍只走现有 Turborepo/TypeScript 构建；exporter 需要显式构建，避免每次 IDE 构建都触发 Gradle/Java。
- `packages/exporter` 是迁入后的主开发路径；原兄弟仓仅作为历史来源，不再作为当前工作路径。

## 2026-06-14 · exporter 样本闭环、启动环境结论、迁移交接

详见已归档的 [`session-handoff-2026-06-14.md`](../archive/hand-offs/session-handoff-2026-06-14.md)。

### 已完成

1. **Exporter 运行时最低版本下调并验证**
   - NeoForge compile target 与 runtime min version 拆分。
   - runtime 版本范围已降为 `[21.1.1,)`。
   - 在 `neoforge@21.1.227` 的 Labpack 实例中 exporter 能加载。

2. **Exporter 配方导出容错**
   - TACZ `tacz:gun_smith_table_crafting` 的 codec 会抛 `UnsupportedOperationException`。
   - exporter 已改为单配方 fail-open：坏配方写入 `recipes`，`raw_json = NULL`，`unparsed = 1`，整次导出继续。
   - 相关 exporter commit：`ce7d14b fix: tolerate recipe export failures`。

3. **Labpack 样本导出成功**
   - 样本：`/Volumes/SSD-1/Transmation/export.sqlite`。
   - `PRAGMA integrity_check` 返回 `ok`。
   - 数据规模：194 mods、8295 items、6488 blocks、10341 recipes、31301 recipe_inputs、8005 recipe_outputs、365731 translations。
   - 173 条 `raw_json IS NULL` 全部来自 TACZ 枪匠台配方，符合容错预期。
   - 结构化 input/output 对 item 表无缺失引用。

4. **IDE 当前启动状态确认**
   - `pnpm build` 通过。
   - `pnpm typecheck` 通过。
   - 仓库位于 ExFAT 外置盘时，Electron `.app` bundle 不能可靠安装到 `node_modules`，默认 `pnpm dev` 会失败。
   - 使用 APFS 缓存中的 Electron 二进制可启动到主进程初始化、IPC 注册、renderer 加载和 preload 暴露成功。

### 待办（下一步）

- monorepo 已在 APFS 本机路径，exporter 已并入 `packages/exporter`。
- 实现 IDE importer v1：读取 exporter `export.sqlite`，导入 mods/items/blocks/tags/translations/recipes/recipe_inputs/recipe_outputs，并让物品/配方浏览器基于新数据可用。

## 2026-06-13 · 现状审计 → 清理 → exporter 重写决策与骨架

### 已完成

1. **代码现状审计**（已归档到 `docs/archive/audits/现状审计-2026-06-13.md`）
   - 基于真实代码核实：能跑的是"导入附属 mod 预导出的 export.sqlite → 浏览物品/配方"；规格核心（语义 Agent、unify 引擎、置信×风险、决策清单/diff、KubeJS 产物）**一行未写**。
   - 数据来源 = Delightify-Exporter（附属 mod），**非 JAR 解析**。

2. **死代码清理**（commit `5053c74`）
   - 删 `services/database/batch-save.ts`（零引用、列对不上）。
   - 删两套旧 JAR 字节码分析器 `java-tools/`、`java/mod-inspector/`（TS 端零调用，~7.1M）。
   - `services/llm/` 加 README 标注"孤立、provider 可复用、待按设计/09 重写为 Agent"。

3. **数据来源架构决策**
   - 确认：脚本化整合包（KubeJS/CraftTweaker）的**最终态本就是运行时产物**，离线无法健全求值 → **exporter 是 `data:*` 事实的权威来源**；离线 JAR 解析降级为预览/差集/资源提取的辅助层；规格 06/10 的"离线优先"判定为过时。

4. **Exporter 契约 v1**（`exporter-contract-v1.md`，commit `c2a9fa8`）
   - 性能硬要求 + 解耦 manifest（loader/mc_version/modlist_hash）+ 结构化配方（recipe_inputs/outputs）+ 组件化（components_json）+ 翻译独立表 + 全面物品确定性事实。

5. **Exporter 重写（先独立 repo，后并入本仓）**
   - 决策：放弃增量迁移，**重写，优先 1.21.1 NeoForge**（跨 1.20.5 Data Components 断层 + 修性能 + 对齐契约）；采集逻辑与平台解耦，留 1.20.1 余地。
   - 当前主路径：`packages/exporter`（modid `mpide_exporter`，命令 `/mpide_export dump`）。
   - 骨架**已通过 `./gradlew build`**：NeoForge 21.1.233 / ModDevGradle 2.0.141 / sqlite-jdbc 3.53.2.0(jarJar)；命令注册、契约 v1 全表 DDL、异步导出架构（主线程最小快照→worker 写库→原子改名）、manifest 已实现（含真实 modlist_hash）。
   - 性能根因已坐实并在设计上治掉：旧 exporter 全程跑服务端主线程致卡顿；逐物品建毁 FBO + 同步 glReadPixels。

### 待办（下一步）

- **exporter sources（按序）**：`ModListSource`+`ItemRegistrySource`（端到端导出真实 mods/items，runClient 验证）→ `ItemTagSource`+`TranslationSource` → `RecipeSource`（1.21 RecipeManager + codec + 结构化 inputs/outputs + 组件化，需先做 API 调研）→ 资源/贴图（离线化）+ recipe_views。
- **IDE importer 按契约改造**：读结构化配方/translations/recipe_views、保留全部 item_resources 类型、事实带 provenance。
- exporter 尚未在游戏内 `runClient` 实跑验证。

### 待决策

- exporter `blocks` 表字段范围；`recipe_inputs.role` 是否需 fluid/energy；贴图离线提取可行性边界（需小样本验证）。
- exporter 已并入 Delightify monorepo；独立远端不再是当前必要条件。
